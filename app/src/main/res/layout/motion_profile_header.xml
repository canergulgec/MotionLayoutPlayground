<?xml version="1.0" encoding="utf-8"?>

<com.caner.motionlayoutplayground.utils.CollapsibleToolbar xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/motionLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="false"
    android:minHeight="?actionBarSize"
    android:background="@color/colorGray"
    app:layoutDescription="@xml/scene_profile"
    app:layout_scrollFlags="scroll|enterAlways|snap|exitUntilCollapsed">

    <View
        android:id="@+id/background"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        tools:ignore="ContentDescription" />

    <ImageView
        android:id="@+id/ivProfile"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:scaleType="centerCrop"
        tools:ignore="ContentDescription" />

    <TextView
        android:id="@+id/label"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginTop="@dimen/margin_16"
        android:gravity="center_vertical"
        android:text="John Doe"
        android:textColor="#FFF"
        android:textSize="21sp" />

    <LinearLayout
        android:id="@+id/contactContainer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/ivMail"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:scaleType="centerCrop"
            app:srcCompat="@drawable/ic_mail"
            tools:ignore="ContentDescription" />

        <ImageView
            android:id="@+id/ivCall"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:layout_marginStart="@dimen/margin_24"
            android:scaleType="centerCrop"
            app:srcCompat="@drawable/ic_phone"
            tools:ignore="ContentDescription" />

    </LinearLayout>

    <ImageView
        android:id="@+id/ivBack"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:scaleType="centerCrop"
        app:srcCompat="@drawable/ic_back_button"
        tools:ignore="ContentDescription" />

</com.caner.motionlayoutplayground.utils.CollapsibleToolbar>